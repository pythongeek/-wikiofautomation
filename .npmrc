# Suppress noisy native-module postinstalls that fail first-attempt on 
# Hostinger's stale prebuild mirror (esbuild strict validator, etc.).
# The postinstall script in package.json rebuilds only better-sqlite3.
# On Windows dev, use: npm install --ignore-scripts 
# ignore-scripts=true